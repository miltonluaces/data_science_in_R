# IMPORTS ===========================================================================================================================================================================================

library(R6)
library(MiscFunctions)

# PREPROCESS ===========================================================================================================================================================================================



# PCA ===========================================================================================================================================================================================

#data = cars[,c('sym','height', 'rpm')]

#fit = princomp(data, cor=TRUE)
#summary(fit); loadings(fit) 

# RESULTS ===========================================================================================================================================================================================

GetResults <- function(fit) {
  par(mfrow=c(1,2))
  plot(fit,type="l", main='Components contribution') 
  biplot(fit, main='Biplot 2 PCAs')
  par(mfrow=c(1,1))
  fit$scale
}

